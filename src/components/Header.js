import React from 'react';

const Header = () => {
  return (
    <header className="fixed top-0 left-0 w-full bg-white shadow p-4 z-50"> {/* Add z-50 here */}
      {/* Add your navigation links here */}
    </header>
  );
};

export default Header;
